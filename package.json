{
  "name": "gymp-project",
  "version": "1.0.0",
  "description": "GYM Project Application",
  "engines": {
    "node": "18.x",
    "npm": "10.x"
  },
  "scripts": {
    "start": "cd server && npm install && node server.js",
    "heroku-postbuild": "cd front && npm install && npm run build && cd ../server && npm install",
    "build": "cd front && npm install && npm run build",
    "dev": "cd server && npm run dev",
    "install-all": "npm install && cd front && npm install && cd ../server && npm install"
  },
  "cacheDirectories": [
    "node_modules",
    "front/node_modules",
    "server/node_modules"
  ],
  "dependencies": {
    "express": "^4.18.2",
    "cors": "^2.8.5",
    "mongoose": "^7.0.0",
    "dotenv": "^16.0.3",
    "path": "^0.12.7"
  }
}

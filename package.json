{
  "name": "gymp-project",
  "version": "1.0.0",
  "description": "GYM Project Application",
  "engines": {
    "node": "18.x"
  },
  "scripts": {
    "start": "cd server && npm install && node server.js",
    "heroku-postbuild": "cd front && npm install && npm run build",
    "build": "cd front && npm install && npm run build"
  },
  "cacheDirectories": [
    "node_modules",
    "front/node_modules",
    "server/node_modules"
  ]
}
